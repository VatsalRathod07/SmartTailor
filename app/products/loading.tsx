import React from 'react';

const Loading = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <h1 className="animate-bounce text-black">Smart Tailor & Cloth</h1>
        </div>
    );
};

export default Loading;
