import React from 'react';

const Loading = () => {
    return (
        <div className="flex items-center justify-center h-screen">
            <img src="/img/loading.png" alt="" className="w-52 object-cover animate-bounce" />
        </div>
    );
};

export default Loading;
